@model Products.Models.Category

@{
    ViewData["Title"] = "Details";
}

<div class="card">
    <div class="card-header">
        <h5> Details Category</h5>
    </div>
    <div class="card-body">
      
        <hr />
        <dl class="row">
            <dt class="col-sm-2">
                @Html.DisplayNameFor(model => model.CategoryName)
            </dt>
            <dd class="col-sm-10">
                @Html.DisplayFor(model => model.CategoryName)
            </dd>
        </dl>
        <div>
            <a asp-action="Edit" asp-route-id="@Model.Id" class="btn btn-primary btn-sm edit-btn"><i class="fa-solid fa-pen-to-square"></i> Edit</a>
            <a asp-action="Index" style="margin-left: 10px;" class="btn btn-danger btn-sm"> <i class="fa fa-arrow-left"></i>>Back to List</a>
        </div>
    </div>
   
</div>
